# Requirements for CHAOS Framework

# Core dependencies
dataclasses  # Built-in for Python 3.7+
json         # Built-in
random       # Built-in
datetime     # Built-in
enum         # Built-in

# Enhanced generation with Gemini AI
google-generativeai>=0.8.0

# Optional - for different output formats and analysis
pandas>=1.3.0        # For data analysis
numpy>=1.21.0        # For numerical operations
tqdm>=4.65.0         # For progress bars in large generations

# Optional - for PEFT training (install separately if needed)
# transformers>=4.30.0
# peft>=0.4.0
# torch>=2.0.0
# datasets>=2.12.0
# accelerate>=0.20.0
